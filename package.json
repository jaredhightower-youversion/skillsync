{
  "name": "skillsync",
  "version": "0.3.0",
  "private": true,
  "description": "Version and changelog live here; changesets manages both. The program itself is Go, see go.mod.",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1"
  }
}
